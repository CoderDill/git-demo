git checkout -b

different algorithm for merging

git checkout

get branch -d

Merge conflicts happen when changes have been made to the branch you're trying to merge into.

git checkout -b secondPart

Making changes on master to create a merge conflict with secondPart

Merge created and fixed.

